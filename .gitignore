# Local task data. The database now lives in the OS app-data directory by
# default, so these are backstops in case someone points TASKTRACKER_DB here.
tasks.json
*.db
*.db-wal
*.db-shm

# Build artifacts
tasktracker
tasktracker.exe
*.exe

# OS junk
Thumbs.db
.DS_Store
